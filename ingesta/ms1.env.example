# Copiar exactamente la conexión de MS1; no se asumen credenciales por defecto.
# Desde Docker local, usar host.docker.internal si PostgreSQL está en el host.
# En VM ingesta, usar el DNS/IP alcanzable de la VM de bases de datos.
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=

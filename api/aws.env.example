# Mismas credenciales temporales del Learner Lab personal que usa
# ingesta/aws.env (se pueden copiar tal cual). Nunca subir aws.env a Git.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_REGION=us-east-1
